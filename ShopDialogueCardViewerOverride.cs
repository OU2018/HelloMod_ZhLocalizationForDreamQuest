using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace HelloMod
{
    internal class ShopDialogueCardViewerOverride
    {
        //没有必要
        /*public static void SetName(CardData s,ShopDialogueCardViewer __instance)
        {
            __instance.text.text = s.realName;
        }

        public virtual void CardName(ref string __result, ShopDialogueCardViewer __instance)
        {
            if (!string.IsNullOrEmpty(__result))
            {
                __result = ;
            }
            return (__instance.activeCard == null) ? string.Empty : __instance.activeCard.realName;
        }

        public virtual void CardDescription(ref string __result, ShopDialogueCardViewer __instance)
        {
            if (!string.IsNullOrEmpty(__result))
            {
                __result = ;
            }
            return (__instance.activeCard == null) ? string.Empty : __instance.activeCard.realName;
        }*/
    }
}
